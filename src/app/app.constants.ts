// global http get cache storeage id
export const CACHE_KEY = 'horton-entry-http-get-cache';

// during development I may need more aggressive caching, since the api is rate limited
export const BROWSER_CACHE_STORAGE = 'sessionStorage';

export const API_URL = 'https://api.github.com';

export const REPO_ITEMS_PER_PAGE = 10;

export const ISSUE_ITEMS_PER_PAGE = 10;
